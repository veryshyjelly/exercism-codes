# Get all scores
# Scores ? Scores
Scores ← |1 ∘

# Get the latest score
# Score ? Scores
Latest ← |1 ⊣

# Get the top score
# TopScore ? Scores
PersonalBest ← |1 /↥

# Get the top three scores
# TopThreeScores ? Scores
PersonalTopThree ← |1 ⍣(↙₃|∘)⇌⍆
