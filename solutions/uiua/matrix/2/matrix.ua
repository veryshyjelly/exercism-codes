Matrix ← ⊜(⊜⋕(⊸≠@ )) ⊸≠@\n
# Parse a row from a matrix
# RowValues ? Matrix RowIndex
Row ← |2 ⊡ -1 : Matrix

# Parse a column from a matrix
# ColumnValues ? Matrix ColumnIndex
Column ← |2 ≡⊡ -1 : Matrix
